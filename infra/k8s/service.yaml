apiVersion: v1
kind: Service
metadata:
  name: vulnerable-app-service
spec:
  # VULNERABILITY: Exposing service externally without restrictions
  type: LoadBalancer
  selector:
    app: vulnerable-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
